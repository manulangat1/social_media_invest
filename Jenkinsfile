pipeline { 
    agent any 

    stages {
        stage("Init") { 
            steps{ 
                sh "This is the init stage"
            }
        }

        stage ("Get the package version and bump it"){
            steps { 
                script{
                    sh "Hello world"
                }
            }
        }
    }
}